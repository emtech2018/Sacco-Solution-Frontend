export interface image{
    fromDate?: Date,
    image?: string,
    modifiedBy?: string,
    modifiedOn?: Date,
    postedBy?: string,
    postedFlag?: string,
    postedTime?: Date,
    rcre?: Date,
    sn?: number,
    toDate?: Date
  }